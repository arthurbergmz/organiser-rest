{
  "name": "organiser-rest",
  "version": "0.0.1",
  "description": "Example REST API architecture for Organiser.",
  "main": "index.js",
  "scripts": {
    "start": "echo \"Error: this method is not ready yet\" && exit 1",
    "build": "echo \"Error: this method is not ready yet\" && exit 1",
    "dev": "echo \"Error: this method is not ready yet\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "organiser": "file:../organiser"
  },
  "engines": {
    "node": ">= 10.0.0"
  },
  "engineStrict": true
}
